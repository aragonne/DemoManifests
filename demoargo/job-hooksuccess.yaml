apiVersion: batch/v1
kind: Job
metadata:
  name: echo-success
  namespace: demoargo
  annotations:
    argocd.argoproj.io/hook: PostSyncs
spec:
  template:
    spec:
      containers:
      - name: echo-presync
        image: alpine
        command: ["echo", "L’application a success."]
      restartPolicy: Never