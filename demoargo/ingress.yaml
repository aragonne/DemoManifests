apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: ingress
  namespace: demoargo
spec:
  rules:
  - host: 127.0.0.1
    http:
      paths:
      - path: /truc
        pathType: Prefix
        backend:
          service:
            name: demo-service
            port:
              number: 80
