apiVersion: batch/v1
kind: Job
metadata:
  name: echo-fail
  namespace: demoargo
  annotations:
    argocd.argoproj.io/hook: SyncFail
spec:
  template:
    spec:
      containers:
      - name: echo-fail
        image: alpine
        command: ["echo", "L’application a fail."]
      restartPolicy: Never